# Риск и распределение инвестиций в стартапы  
*(Startup Applications: Risk Assessment & Investment Allocation)*

## 📌 Описание проекта
Данный проект представляет собой учебный **Data Science pet-project**, моделирующий процесс принятия инвестиционных решений венчурным фондом.

Цель проекта — построить **ML-пайплайн**, который:
1. Оценивает **риск отказа в финансировании** стартапа (классификация);
2. Для одобренных заявок **прогнозирует оптимальный объём инвестиций** (регрессия);
3. Объединяет оба шага в единый, интерпретируемый pipeline, приближённый к реальным инвестиционным решениям.

Проект оформлен как **предлагаемый продукт** для инвестиционной компании и одновременно как **портфолио Data Scientist**.

---

## 🧠 Бизнес-контекст
Инвест-компании ежедневно рассматривают сотни заявок стартапов.  
Задача аналитической системы —:
- отсеивать высокорисковые проекты;
- минимизировать вероятность финансирования неустойчивых стартапов;
- рекомендовать диапазон инвестиций для перспективных заявок.

В данном проекте этот процесс формализован в виде **двухэтапной ML-модели**:
> **Risk filtering → Capital allocation**

---

## 📊 Данные
Используется **синтетический датасет**, сгенерированный по запросу к ChatGPT и имитирующий реальные заявки стартапов.

### Основные признаки:
- **Категориальные:**  
  `startup_stage`, `industry`, `region`
- **Финансовые:**  
  `requested_amount`, `annual_revenue`
- **Оценка бизнеса:**  
  `pre_money_valuation`, `market_size_estimate`
- **Команда:**  
  `team_size`, `founders_experience_years`

### Целевая переменная:
- `investment_amount` — фактический объём инвестиций  
  *(0 = отказ в финансировании)*

> ⚠️ Датасет синтетический, однако структура, зависимости и постановка задачи приближены к реальным инвестиционным сценариям.

---

## 🔍 EDA и аналитические выводы
В ходе разведочного анализа были выявлены ключевые закономерности:

### Факторы отказа:
- Ранние стадии стартапа (Idea, Pre-Seed);
- Малый размер команды;
- Низкий опыт основателей;
- Нереалистично высокие инвестиционные запросы.

### Факторы, влияющие на объём инвестиций:
- Запрошенная сумма (`requested_amount`);
- Оценка стартапа до инвестиций (`pre_money_valuation`);
- Размер команды и опыт фаундеров;
- Текущая выручка.

### Практический вывод:
- **TAM (оценка рынка)** почти не влияет ни на вероятность отказа, ни на объём инвестиций;
- Решения инвесторов в первую очередь основаны на **риске команды и финансовой реалистичности**.

---

## 🤖 Модель 1 — Классификация (отказ / принятие)

### Цель
Определить вероятность отказа в финансировании стартапа.

### Подход
Обучены и сравнены модели:
- Logistic Regression  
- Random Forest  
- SVM  
- XGBoost  
- **LightGBM (финальная модель)**

Оптимизация проводилась по метрике **F1-score для класса отказа**, так как:
> В инвестиционном контексте критично **минимизировать пропуск рискованных стартапов (FN)**.

### Итоговые метрики (LightGBM):
- Accuracy: **0.68**
- F1 (отказ): **0.61**
- Recall (отказ): **0.72**
- ROC-AUC: **0.742**
- FN = **89 (лучший результат среди моделей)**

Подобран **вероятностный порог 0.51**, позволяющий контролировать баланс FP/FN.

---

## 💰 Модель 2 — Регрессия (объём инвестиций)

### Цель
Прогнозировать оптимальный объём инвестиций **только для одобренных стартапов**.

### Особенности:
- Целевая переменная логарифмирована для устойчивости к выбросам;
- Использована модель **RandomForestRegressor**.

### Метрики:
**В лог-пространстве:**
- Log-RMSE = **0.175**
- Log-MAE = **0.142**
- R² = **0.967**

**В долларах:**
- MAE ≈ **$363,268**
- RMSE ≈ **$579,173**
- MAPE ≈ **14.6%**

### Интерпретация:
- Модель хорошо предсказывает **порядок величины инвестиций**;
- Результат следует использовать как **рекомендованный диапазон**, а не точную сумму сделки.

---

## 🔗 Полный ML-пайплайн
Финальный pipeline объединяет обе модели:

1. Классификация → вероятность отказа;
2. Решение о финансировании;
3. Регрессия → диапазон инвестиций (predict ± MAE).

### Пример результата:
| ID | Вероятность отказа | Решение   | Инвестиции |
|----|------------------|-----------|------------|
| 0  | 64% | ОТКАЗАНО | ОТКАЗ |
| 2  | 10% | ПРИНЯТО | от $5.3M до $6.05M |
| 4  | 11% | ПРИНЯТО | от $13.9M до $14.6M |

---

## 🗂 Структура проекта
```bash
data/
├─ raw/
└─ processed/

notebooks/
├─ exploration.ipynb
├─ startup_classification.ipynb
├─ startup_sponsoring.ipynb
├─ pipeline.ipynb
└─ test/

src/
└─ models/
├─ interim/
├─ classifier_lgbm.joblib
└─ rf_regressor.joblib
```


---

## 🛠 Стек технологий
`Python`, `pandas`, `numpy`, `scikit-learn`,  
`LightGBM`, `XGBoost`, `RandomForest`,  
`matplotlib`, `seaborn`, `joblib`

---

## 🚧 Ограничения и дальнейшее развитие
- Датасет синтетический — требуется проверка на реальных данных;
- Возможное улучшение:
  - cost-sensitive learning;
  - Bayesian models для uncertainty;
  - временная динамика стартапов;
  - интеграция в web-интерфейс.

---

## 📌 Рекомендации по использованию модели

### 1. Использование классификатора (оценка риска)
Модель классификации возвращает **вероятность отказа в финансировании**.

В проекте используется **порог вероятности = 0.51**:
- `P(отказ) ≥ 0.51` → **ОТКАЗАНО**
- `P(отказ) < 0.51` → **ПРИНЯТО**

Данный порог был подобран эмпирически на валидационной выборке с целью:
- минимизировать количество **ложных принятий (FN)**;
- сохранить приемлемый уровень отказов перспективным стартапам.

---

### 2. Использование регрессии (объём инвестиций)
Модель регрессии применяется **только к стартапам, прошедшим классификационный фильтр**.

Важно:
- модель **не предназначена для точного определения суммы сделки**;
- прогноз следует интерпретировать как **ориентир**.

В проекте рекомендуется использовать **диапазон инвестиций**:


## 👤 Автор

*@Ejtolf*

- [GitHub](https://github.com/Ejtolf)
- [Kaggle](https://kaggle.com/arraylist1402)

---
